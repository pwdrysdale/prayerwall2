query Query($id: Float!) {
    onePrayer(id: $id) {
        id
        title
        body
        privat
        category
        answered
        comments {
            id
        }
        lists {
            id
            name
            description
            privat
        }
    }
    myLists {
        id
        name
        privat
    }
}
