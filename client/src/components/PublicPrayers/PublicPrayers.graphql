query Query($cursor: String) {
    publicPrayers(cursor: $cursor) {
        title
        body
        category
        answered
        privat
        user {
            username
        }
        createdDate
    }
}
