query Query($cursor: String) {
    publicPrayers(cursor: $cursor) {
        id
        title
        body
        category
        answered
        privat
        user {
            username
            id
        }
        comments {
            id
        }
        createdDate
    }
    me {
        id
        username
    }
}
